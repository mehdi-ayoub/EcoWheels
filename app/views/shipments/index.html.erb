<h1>Show all shipments</h1>

<div class="container">
 <div class="row justify-content-center">

    <%= form_with url: shipments_path, method: :get, class: "d-flex my-3" do %>
      <% text_field_tag :query,
       params[:query],
       class: "form-control",
       placeholder: "Find a shipment"
      %>
      <%= submit_tag "Search", class: "btn btn-primary" %>
    <% end %>
 </div>
 </div>

  <%# <% @shipments.each do |shipment|
    <div>
  <h3><%= shipment.city</h3>
  <%# <% end %>
<%# </div> %>

<%# id="shipments">
  <% if @shipments.any? %>
